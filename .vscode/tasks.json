{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Compile",
            "type": "shell",
            "command": "g++",
            "args": ["-g", "Car.cpp", "main.cpp", "-o", "${workspaceFolder}/myprogram.exe"],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}